[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "jury-eval"
version = "0.1.0"
description = "Unified LLM evaluation: A/B uplift, Panel/Jury judges, Krippendorff α, Cohen κ, pairwise preference, Elo tournament, and bias detection"
readme = { file = "README.md", content-type = "text/markdown" }
license = { file = "LICENSE" }
requires-python = ">=3.12"
authors = [
    { name = "William Caban", email = "william.caban@gmail.com" },
]
keywords = [
    "llm", "evaluation", "llm-evaluation", "ai-evaluation",
    "inter-rater-agreement", "krippendorff", "cohen-kappa",
    "pairwise", "elo", "tournament", "judge", "rubric",
    "bias-detection", "positional-bias", "verbosity-bias",
    "agent-skills-eval", "mlflow",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Software Development :: Quality Assurance",
    "Topic :: Software Development :: Testing",
    "Typing :: Typed",
]
dependencies = [
    "pydantic>=2.0",
    "numpy>=1.26",
    "scipy>=1.13",
    "scikit-learn>=1.5",
    "krippendorff>=0.6",
    "pyyaml>=6.0",
]

[project.urls]
Homepage      = "https://github.com/williamcaban/JuryEval"
Repository    = "https://github.com/williamcaban/JuryEval"
Documentation = "https://github.com/williamcaban/JuryEval/tree/main/docs"
Issues        = "https://github.com/williamcaban/JuryEval/issues"
Changelog     = "https://github.com/williamcaban/JuryEval/blob/main/CHANGELOG.md"

[project.scripts]
jury-eval = "jury_eval.cli.main:app"

[project.optional-dependencies]
openai    = ["openai>=1.30"]
anthropic = ["anthropic>=0.28"]
all       = ["jury-eval[openai,anthropic]"]

# uv-native dev dependencies (uv sync --group dev)
[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.4",
    "mypy>=1.10",
]

[tool.hatch.build.targets.wheel]
packages = ["src/jury_eval"]

[tool.hatch.build.targets.sdist]
include = [
    "src/",
    "tests/",
    "docs/",
    "examples/",
    "README.md",
    "LICENSE",
    "CHANGELOG.md",
    "pyproject.toml",
]

[tool.pytest.ini_options]
testpaths    = ["tests"]
addopts      = "--tb=short -q"
python_files = ["test_*.py"]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select  = ["E", "F", "I", "UP", "B", "SIM"]
ignore  = ["E501"]

[tool.mypy]
strict              = true
ignore_missing_imports = true

[tool.coverage.run]
source = ["src/jury_eval"]
omit   = [
    "src/jury_eval/llm/openai_backend.py",
    "src/jury_eval/llm/anthropic_backend.py",
]

[tool.coverage.report]
show_missing = true
skip_covered = false
fail_under   = 70
